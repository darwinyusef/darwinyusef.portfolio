---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import { getLangFromUrl } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
---

<Layout title={lang === 'es' ? 'Arquitectura de Software' : lang === 'en' ? 'Software Architecture' : 'Arquitetura de Software'}>
  <div class="min-h-screen bg-[#F8F8F8] dark:bg-[#0a0a0a]">
    <Header />

    <main class="overflow-hidden px-5">
      <!-- Hero Section con Ultra Large Text -->
      <section class="relative min-h-[70vh] flex items-center justify-center py-20">
        <div class="max-w-[1800px] mx-auto w-full">
          <div class="text-center">
            <h1 class="text-[clamp(3rem,12vw,12rem)] font-black leading-[0.9] tracking-tighter text-gray-900 dark:text-white mb-8" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? 'ARQUITECTURA' : lang === 'en' ? 'ARCHITECTURE' : 'ARQUITETURA'}
              <br />
              <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary via-purple-600 to-pink-600">
                {lang === 'es' ? 'DE SOFTWARE' : lang === 'en' ? 'SOFTWARE' : 'DE SOFTWARE'}
              </span>
            </h1>
            <p class="text-xl md:text-2xl text-gray-600 dark:text-gray-400 max-w-3xl mx-auto leading-relaxed" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es'
                ? 'Sistemas escalables diseñados para crecer sin límites'
                : lang === 'en'
                ? 'Scalable systems designed to grow without limits'
                : 'Sistemas escaláveis projetados para crescer sem limites'}
            </p>
          </div>
        </div>
      </section>

      <!-- Content Section -->
      <section class="pb-20 max-w-5xl mx-auto">
        <div class="bg-white dark:bg-[#1a1a1a] rounded-3xl shadow-xl p-8 md:p-12 space-y-12">

          <!-- Intro -->
          <div class="space-y-6">
            <h2 class="text-3xl md:text-4xl font-black text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? 'Arquitectura que escala con tu negocio' : lang === 'en' ? 'Architecture that scales with your business' : 'Arquitetura que escala com seu negócio'}
            </h2>

            <p class="text-lg text-gray-600 dark:text-gray-400 leading-relaxed">
              {lang === 'es'
                ? 'No hay nada más costoso que rediseñar toda tu plataforma porque la arquitectura inicial no aguantó el crecimiento.'
                : lang === 'en'
                ? "There's nothing more expensive than redesigning your entire platform because the initial architecture couldn't handle growth."
                : 'Não há nada mais caro do que redesenhar toda sua plataforma porque a arquitetura inicial não aguentou o crescimento.'}
            </p>

            <div class="space-y-3">
              <p class="text-lg font-semibold text-gray-900 dark:text-white">
                {lang === 'es' ? 'Diseñamos sistemas que:' : lang === 'en' ? 'We design systems that:' : 'Projetamos sistemas que:'}
              </p>
              <ul class="space-y-2 text-gray-600 dark:text-gray-400">
                <li class="flex items-start gap-3">
                  <span class="text-primary font-bold">✓</span>
                  <span>{lang === 'es' ? 'Escalan de 100 a 1 millón de usuarios sin reescribir código' : lang === 'en' ? 'Scale from 100 to 1 million users without rewriting code' : 'Escalam de 100 a 1 milhão de usuários sem reescrever código'}</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-primary font-bold">✓</span>
                  <span>{lang === 'es' ? 'Se mantienen fácilmente aunque cambien desarrolladores' : lang === 'en' ? 'Remain easily maintainable even when developers change' : 'Mantêm-se facilmente mesmo quando desenvolvedores mudam'}</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-primary font-bold">✓</span>
                  <span>{lang === 'es' ? 'Tienen alta disponibilidad (99.9%+ uptime)' : lang === 'en' ? 'Have high availability (99.9%+ uptime)' : 'Têm alta disponibilidade (99.9%+ uptime)'}</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-primary font-bold">✓</span>
                  <span>{lang === 'es' ? 'Permiten agregar features sin romper lo existente' : lang === 'en' ? 'Allow adding features without breaking existing code' : 'Permitem adicionar recursos sem quebrar o existente'}</span>
                </li>
              </ul>
            </div>
          </div>

          <div class="border-t-2 border-gray-200 dark:border-gray-800"></div>

          <!-- Metodología: Arquitectura de Decisiones + Análisis + TOGAF + Agile -->
          <div class="space-y-6">
            <h2 class="text-3xl md:text-4xl font-black text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? 'Nuestro abordaje metodológico' : lang === 'en' ? 'Our methodological approach' : 'Nossa abordagem metodológica'}
            </h2>

            <div class="bg-gradient-to-br from-primary/5 to-purple-600/5 dark:from-primary/10 dark:to-purple-600/10 rounded-2xl p-6 md:p-8 space-y-6">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-primary to-purple-600 flex items-center justify-center text-white text-2xl font-bold">
                  🏛️
                </div>
                <h3 class="text-2xl font-bold text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
                  {lang === 'es' ? 'TOGAF® + Arquitectura Empresarial Ágil' : lang === 'en' ? 'TOGAF® + Agile Enterprise Architecture' : 'TOGAF® + Arquitetura Empresarial Ágil'}
                </h3>
              </div>

              <p class="text-lg text-gray-700 dark:text-gray-300 leading-relaxed">
                {lang === 'es'
                  ? 'Desde el abordaje metodológico del Open Group y el framework de TOGAF®, la arquitectura empresarial ágil implica llevar a cabo ejercicios de arquitectura empresarial con capacidad de respuesta al cambio, enfocados permanentemente a la entrega de valor al negocio de manera continua y sostenible.'
                  : lang === 'en'
                  ? 'From the Open Group\'s methodological approach and the TOGAF® framework, agile enterprise architecture involves conducting enterprise architecture exercises with the ability to respond to change, permanently focused on delivering business value in a continuous and sustainable manner.'
                  : 'Da abordagem metodológica do Open Group e do framework TOGAF®, a arquitetura empresarial ágil implica realizar exercícios de arquitetura empresarial com capacidade de resposta à mudança, focados permanentemente na entrega de valor ao negócio de forma contínua e sustentável.'}
              </p>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <!-- Sprint de Arquitectura -->
                <div class="bg-white dark:bg-[#1a1a1a] rounded-xl p-5 border-2 border-gray-200 dark:border-gray-700">
                  <div class="flex items-center gap-2 mb-3">
                    <span class="text-2xl">⚡</span>
                    <h4 class="font-bold text-gray-900 dark:text-white">
                      {lang === 'es' ? 'Sprints de Arquitectura' : lang === 'en' ? 'Architecture Sprints' : 'Sprints de Arquitetura'}
                    </h4>
                  </div>
                  <p class="text-sm text-gray-600 dark:text-gray-400">
                    {lang === 'es'
                      ? 'Experimentación práctica y puesta en operación de soluciones mínimas viables (MVP) de forma iterativa e incremental.'
                      : lang === 'en'
                      ? 'Practical experimentation and operationalization of minimum viable solutions (MVP) in an iterative and incremental manner.'
                      : 'Experimentação prática e operacionalização de soluções mínimas viáveis (MVP) de forma iterativa e incremental.'}
                  </p>
                </div>

                <!-- Células Ágiles -->
                <div class="bg-white dark:bg-[#1a1a1a] rounded-xl p-5 border-2 border-gray-200 dark:border-gray-700">
                  <div class="flex items-center gap-2 mb-3">
                    <span class="text-2xl">🎯</span>
                    <h4 class="font-bold text-gray-900 dark:text-white">
                      {lang === 'es' ? 'Células Ágiles de Arquitectura' : lang === 'en' ? 'Agile Architecture Cells' : 'Células Ágeis de Arquitetura'}
                    </h4>
                  </div>
                  <p class="text-sm text-gray-600 dark:text-gray-400">
                    {lang === 'es'
                      ? 'Equipos empoderados y autónomos, armonizados bajo la gobernanza de la arquitectura empresarial.'
                      : lang === 'en'
                      ? 'Empowered and autonomous teams, harmonized under enterprise architecture governance.'
                      : 'Equipes empoderadas e autônomas, harmonizadas sob a governança da arquitetura empresarial.'}
                  </p>
                </div>

                <!-- Colaboración Continua -->
                <div class="bg-white dark:bg-[#1a1a1a] rounded-xl p-5 border-2 border-gray-200 dark:border-gray-700">
                  <div class="flex items-center gap-2 mb-3">
                    <span class="text-2xl">🤝</span>
                    <h4 class="font-bold text-gray-900 dark:text-white">
                      {lang === 'es' ? 'Colaboración con Stakeholders' : lang === 'en' ? 'Stakeholder Collaboration' : 'Colaboração com Stakeholders'}
                    </h4>
                  </div>
                  <p class="text-sm text-gray-600 dark:text-gray-400">
                    {lang === 'es'
                      ? 'Equipos en continua comunicación y colaboración con stakeholders e implementadores, manteniendo el valor al negocio.'
                      : lang === 'en'
                      ? 'Teams in continuous communication and collaboration with stakeholders and implementers, maintaining business value.'
                      : 'Equipes em comunicação contínua e colaboração com stakeholders e implementadores, mantendo o valor ao negócio.'}
                  </p>
                </div>

                <!-- Personas primero -->
                <div class="bg-white dark:bg-[#1a1a1a] rounded-xl p-5 border-2 border-gray-200 dark:border-gray-700">
                  <div class="flex items-center gap-2 mb-3">
                    <span class="text-2xl">👥</span>
                    <h4 class="font-bold text-gray-900 dark:text-white">
                      {lang === 'es' ? 'Las personas primero' : lang === 'en' ? 'People first' : 'Pessoas primeiro'}
                    </h4>
                  </div>
                  <p class="text-sm text-gray-600 dark:text-gray-400">
                    {lang === 'es'
                      ? 'A las personas se las trata con respeto y están por encima de metodologías, procesos y herramientas.'
                      : lang === 'en'
                      ? 'People are treated with respect and are above methodologies, processes, and tools.'
                      : 'As pessoas são tratadas com respeito e estão acima de metodologias, processos e ferramentas.'}
                  </p>
                </div>
              </div>

              <div class="bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-primary/30">
                <p class="text-base text-gray-700 dark:text-gray-300 leading-relaxed">
                  <span class="font-bold text-primary">
                    {lang === 'es' ? '💡 Nuestro enfoque:' : lang === 'en' ? '💡 Our approach:' : '💡 Nossa abordagem:'}
                  </span>
                  {' '}
                  {lang === 'es'
                    ? 'Permitimos los cambios, la experimentación práctica y la entrega continua de valor al negocio de forma sostenible, donde la capacidad de respuesta al cambio es fundamental para el éxito de tu arquitectura empresarial.'
                    : lang === 'en'
                    ? 'We enable changes, practical experimentation, and continuous delivery of business value in a sustainable way, where the ability to respond to change is fundamental to the success of your enterprise architecture.'
                    : 'Permitimos mudanças, experimentação prática e entrega contínua de valor ao negócio de forma sustentável, onde a capacidade de resposta à mudança é fundamental para o sucesso de sua arquitetura empresarial.'}
                </p>
              </div>
            </div>
          </div>

          <div class="border-t-2 border-gray-200 dark:border-gray-800"></div>

          <!-- Patrones de Arquitectura -->
          <div class="space-y-8">
            <h2 class="text-3xl md:text-4xl font-black text-gray-900 dark:text-white text-center" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? 'Patrones de arquitectura que usamos' : lang === 'en' ? 'Architecture patterns we use' : 'Padrões de arquitetura que usamos'}
            </h2>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
              <!-- Hexagonal Card -->
              <button
                onclick="openModal('modal-hexagonal')"
                class="bg-gradient-to-br from-primary/10 to-purple-600/10 dark:from-primary/20 dark:to-purple-600/20 rounded-2xl p-6 text-center hover:scale-105 transition-transform duration-300 cursor-pointer border-2 border-transparent hover:border-primary"
              >
                <div class="text-5xl mb-4">🏗️</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Hexagonal' : lang === 'en' ? 'Hexagonal' : 'Hexagonal'}
                </h3>
                <p class="text-sm text-gray-600 dark:text-gray-400 mt-2">
                  {lang === 'es' ? 'Ports & Adapters' : lang === 'en' ? 'Ports & Adapters' : 'Ports & Adapters'}
                </p>
              </button>

              <!-- Clean Architecture Card -->
              <button
                onclick="openModal('modal-clean')"
                class="bg-gradient-to-br from-purple-600/10 to-pink-600/10 dark:from-purple-600/20 dark:to-pink-600/20 rounded-2xl p-6 text-center hover:scale-105 transition-transform duration-300 cursor-pointer border-2 border-transparent hover:border-primary"
              >
                <div class="text-5xl mb-4">🧅</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Clean Architecture' : lang === 'en' ? 'Clean Architecture' : 'Clean Architecture'}
                </h3>
                <p class="text-sm text-gray-600 dark:text-gray-400 mt-2">
                  {lang === 'es' ? 'Onion Architecture' : lang === 'en' ? 'Onion Architecture' : 'Onion Architecture'}
                </p>
              </button>

              <!-- Microservicios Card -->
              <button
                onclick="openModal('modal-microservices')"
                class="bg-gradient-to-br from-pink-600/10 to-primary/10 dark:from-pink-600/20 dark:to-primary/20 rounded-2xl p-6 text-center hover:scale-105 transition-transform duration-300 cursor-pointer border-2 border-transparent hover:border-primary"
              >
                <div class="text-5xl mb-4">🔷</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Microservicios' : lang === 'en' ? 'Microservices' : 'Microserviços'}
                </h3>
                <p class="text-sm text-gray-600 dark:text-gray-400 mt-2">
                  {lang === 'es' ? 'Servicios independientes' : lang === 'en' ? 'Independent services' : 'Serviços independentes'}
                </p>
              </button>
            </div>
          </div>

          <div class="border-t-2 border-gray-200 dark:border-gray-800"></div>

          <!-- Ciclo de vida del software -->
          <div class="space-y-8">
            <h2 class="text-3xl md:text-4xl font-black text-gray-900 dark:text-white text-center" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? 'Ciclo de vida del software' : lang === 'en' ? 'Software lifecycle' : 'Ciclo de vida do software'}
            </h2>

            <div class="relative">
              <!-- Línea vertical -->
              <div class="absolute left-8 top-0 bottom-0 w-0.5 bg-gradient-to-b from-primary via-purple-600 to-pink-600"></div>

              <!-- Timeline items -->
              <div class="space-y-8 relative">
                <!-- 1. Caracterización y revisión de requerimientos -->
                <div class="flex gap-6 items-start">
                  <div class="flex-shrink-0 w-16 h-16 rounded-full bg-primary flex items-center justify-center text-white text-2xl font-bold relative z-10">
                    1
                  </div>
                  <div class="flex-1 bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-gray-200 dark:border-gray-700">
                    <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
                      {lang === 'es' ? 'Caracterización y revisión de requerimientos' : lang === 'en' ? 'Characterization and requirements review' : 'Caracterização e revisão de requisitos'}
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400">
                      {lang === 'es'
                        ? 'Analizamos y documentamos las necesidades del proyecto, identificando objetivos, alcance y funcionalidades clave.'
                        : lang === 'en'
                        ? 'We analyze and document project needs, identifying objectives, scope, and key functionalities.'
                        : 'Analisamos e documentamos as necessidades do projeto, identificando objetivos, escopo e funcionalidades chave.'}
                    </p>
                  </div>
                </div>

                <!-- 2. Diseño del software y mockups -->
                <div class="flex gap-6 items-start">
                  <div class="flex-shrink-0 w-16 h-16 rounded-full bg-purple-600 flex items-center justify-center text-white text-2xl font-bold relative z-10">
                    2
                  </div>
                  <div class="flex-1 bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-gray-200 dark:border-gray-700">
                    <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
                      {lang === 'es' ? 'Diseño del software y diseño de mockups/wireframes' : lang === 'en' ? 'Software design and mockups/wireframes design' : 'Design de software e design de mockups/wireframes'}
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400">
                      {lang === 'es'
                        ? 'Creamos la estructura del software y diseñamos interfaces visuales con mockups y wireframes para validar la experiencia del usuario.'
                        : lang === 'en'
                        ? 'We create the software structure and design visual interfaces with mockups and wireframes to validate user experience.'
                        : 'Criamos a estrutura do software e desenhamos interfaces visuais com mockups e wireframes para validar a experiência do usuário.'}
                    </p>
                  </div>
                </div>

                <!-- 3. Diseño del prototipo -->
                <div class="flex gap-6 items-start">
                  <div class="flex-shrink-0 w-16 h-16 rounded-full bg-pink-600 flex items-center justify-center text-white text-2xl font-bold relative z-10">
                    3
                  </div>
                  <div class="flex-1 bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-gray-200 dark:border-gray-700">
                    <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
                      {lang === 'es' ? 'Diseño del prototipo (IA), gestión de agentes y analíticas, gestión de arquitecturas' : lang === 'en' ? 'Prototype design (AI), agent management and analytics, architecture management' : 'Design do protótipo (IA), gestão de agentes e analíticas, gestão de arquiteturas'}
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400">
                      {lang === 'es'
                        ? 'Desarrollamos prototipos funcionales integrando IA, definimos la gestión de agentes inteligentes, configuramos analíticas y establecemos la arquitectura escalable.'
                        : lang === 'en'
                        ? 'We develop functional prototypes integrating AI, define intelligent agent management, configure analytics, and establish scalable architecture.'
                        : 'Desenvolvemos protótipos funcionais integrando IA, definimos a gestão de agentes inteligentes, configuramos analíticas e estabelecemos a arquitetura escalável.'}
                    </p>
                  </div>
                </div>

                <!-- 4. Desarrollo frontend y backend -->
                <div class="flex gap-6 items-start">
                  <div class="flex-shrink-0 w-16 h-16 rounded-full bg-primary flex items-center justify-center text-white text-2xl font-bold relative z-10">
                    4
                  </div>
                  <div class="flex-1 bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-gray-200 dark:border-gray-700">
                    <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
                      {lang === 'es' ? 'Fase de desarrollo frontend y backend (IA), gestión de bases de datos' : lang === 'en' ? 'Frontend and backend development phase (AI), database management' : 'Fase de desenvolvimento frontend e backend (IA), gestão de bases de dados'}
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400">
                      {lang === 'es'
                        ? 'Implementamos el código del frontend y backend con integración de IA, configuramos y optimizamos las bases de datos para máximo rendimiento.'
                        : lang === 'en'
                        ? 'We implement frontend and backend code with AI integration, configure and optimize databases for maximum performance.'
                        : 'Implementamos o código do frontend e backend com integração de IA, configuramos e otimizamos as bases de dados para máximo desempenho.'}
                    </p>
                  </div>
                </div>

                <!-- 5. QA y revisión -->
                <div class="flex gap-6 items-start">
                  <div class="flex-shrink-0 w-16 h-16 rounded-full bg-purple-600 flex items-center justify-center text-white text-2xl font-bold relative z-10">
                    5
                  </div>
                  <div class="flex-1 bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-gray-200 dark:border-gray-700">
                    <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
                      {lang === 'es' ? 'QA, revisión de arquitecturas y planeación' : lang === 'en' ? 'QA, architecture review and planning' : 'QA, revisão de arquiteturas e planejamento'}
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400">
                      {lang === 'es'
                        ? 'Realizamos pruebas exhaustivas de calidad, revisamos la arquitectura implementada y planificamos optimizaciones y mejoras.'
                        : lang === 'en'
                        ? 'We perform thorough quality testing, review the implemented architecture, and plan optimizations and improvements.'
                        : 'Realizamos testes exaustivos de qualidade, revisamos a arquitetura implementada e planejamos otimizações e melhorias.'}
                    </p>
                  </div>
                </div>

                <!-- 6. Despliegue y pruebas finales -->
                <div class="flex gap-6 items-start">
                  <div class="flex-shrink-0 w-16 h-16 rounded-full bg-pink-600 flex items-center justify-center text-white text-2xl font-bold relative z-10">
                    6
                  </div>
                  <div class="flex-1 bg-white dark:bg-[#1a1a1a] rounded-xl p-6 border-2 border-gray-200 dark:border-gray-700">
                    <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
                      {lang === 'es' ? 'Despliegue y pruebas finales con usuarios' : lang === 'en' ? 'Deployment and final user testing' : 'Deploy e testes finais com usuários'}
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400">
                      {lang === 'es'
                        ? 'Desplegamos el sistema a producción y realizamos pruebas finales con usuarios reales para validar funcionalidad y experiencia.'
                        : lang === 'en'
                        ? 'We deploy the system to production and conduct final testing with real users to validate functionality and experience.'
                        : 'Implantamos o sistema em produção e realizamos testes finais com usuários reais para validar funcionalidade e experiência.'}
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="border-t-2 border-gray-200 dark:border-gray-800"></div>

          <!-- Servicios -->
          <div class="space-y-8">
            <h2 class="text-3xl md:text-4xl font-black text-gray-900 dark:text-white text-center" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? 'Servicios que ofrecemos' : lang === 'en' ? 'Services we offer' : 'Serviços que oferecemos'}
            </h2>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              <!-- Evaluación de requerimientos -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">📋</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Evaluación de requerimientos y modelado de datos' : lang === 'en' ? 'Requirements evaluation and data modeling' : 'Avaliação de requisitos e modelagem de dados'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Analizamos necesidades del negocio y diseñamos modelos de datos eficientes y escalables.'
                    : lang === 'en'
                    ? 'We analyze business needs and design efficient, scalable data models.'
                    : 'Analisamos necessidades do negócio e desenhamos modelos de dados eficientes e escaláveis.'}
                </p>
              </div>

              <!-- Diseño de arquitecturas -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">🏛️</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Diseño de arquitecturas validadas por TOGAF o ITIL' : lang === 'en' ? 'Architecture design validated by TOGAF or ITIL' : 'Design de arquiteturas validadas por TOGAF ou ITIL'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Creamos arquitecturas empresariales siguiendo frameworks reconocidos internacionalmente.'
                    : lang === 'en'
                    ? 'We create enterprise architectures following internationally recognized frameworks.'
                    : 'Criamos arquiteturas empresariais seguindo frameworks reconhecidos internacionalmente.'}
                </p>
              </div>

              <!-- Planeación -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">📅</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Planeación con PMP o SCRUM' : lang === 'en' ? 'Planning with PMP or SCRUM' : 'Planejamento com PMP ou SCRUM'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Gestionamos proyectos con metodologías ágiles o tradicionales según las necesidades del equipo.'
                    : lang === 'en'
                    ? 'We manage projects with agile or traditional methodologies according to team needs.'
                    : 'Gerenciamos projetos com metodologias ágeis ou tradicionais conforme as necessidades da equipe.'}
                </p>
              </div>

              <!-- Testing -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">🧪</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Implementación de testing' : lang === 'en' ? 'Testing implementation' : 'Implementação de testing'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Configuramos estrategias de testing automatizado: unitarias, integración, E2E y performance.'
                    : lang === 'en'
                    ? 'We configure automated testing strategies: unit, integration, E2E, and performance.'
                    : 'Configuramos estratégias de testing automatizado: unitárias, integração, E2E e performance.'}
                </p>
              </div>

              <!-- Capacitación -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">👥</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Capacitación de usuarios' : lang === 'en' ? 'User training' : 'Capacitação de usuários'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Entrenamos a tu equipo y usuarios finales para aprovechar al máximo el sistema.'
                    : lang === 'en'
                    ? 'We train your team and end users to get the most out of the system.'
                    : 'Treinamos sua equipe e usuários finais para aproveitar ao máximo o sistema.'}
                </p>
              </div>

              <!-- Documentación -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">📚</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Gestión de documentación' : lang === 'en' ? 'Documentation management' : 'Gestão de documentação'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Creamos y mantenemos documentación técnica clara, completa y actualizada del proyecto.'
                    : lang === 'en'
                    ? 'We create and maintain clear, complete, and up-to-date technical documentation.'
                    : 'Criamos e mantemos documentação técnica clara, completa e atualizada do projeto.'}
                </p>
              </div>

              <!-- Observabilidad -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">👁️</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Observabilidad' : lang === 'en' ? 'Observability' : 'Observabilidade'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Implementamos monitoreo completo: logs, métricas y trazas para identificar problemas antes de que afecten usuarios.'
                    : lang === 'en'
                    ? 'We implement complete monitoring: logs, metrics, and traces to identify issues before they affect users.'
                    : 'Implementamos monitoramento completo: logs, métricas e traces para identificar problemas antes que afetem usuários.'}
                </p>
              </div>

              <!-- Evaluación de servicios cloud -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">☁️</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Evaluación de servicios cloud' : lang === 'en' ? 'Cloud services evaluation' : 'Avaliação de serviços cloud'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Analizamos y recomendamos las mejores soluciones cloud (AWS, Azure, GCP) según tus necesidades y presupuesto.'
                    : lang === 'en'
                    ? 'We analyze and recommend the best cloud solutions (AWS, Azure, GCP) according to your needs and budget.'
                    : 'Analisamos e recomendamos as melhores soluções cloud (AWS, Azure, GCP) conforme suas necessidades e orçamento.'}
                </p>
              </div>

              <!-- Webtrends + Analytics -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">📊</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Webtrends + Analíticas' : lang === 'en' ? 'Webtrends + Analytics' : 'Webtrends + Analytics'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Configuramos herramientas de análisis web y analíticas avanzadas para tomar decisiones basadas en datos.'
                    : lang === 'en'
                    ? 'We configure web analytics tools and advanced analytics to make data-driven decisions.'
                    : 'Configuramos ferramentas de análise web e analytics avançadas para tomar decisões baseadas em dados.'}
                </p>
              </div>

              <!-- Automatización con QA y Claude -->
              <div class="bg-white dark:bg-[#0a0a0a] border-2 border-gray-200 dark:border-gray-800 rounded-2xl p-6 space-y-4 hover:border-primary transition-colors">
                <div class="text-4xl">🤖</div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white">
                  {lang === 'es' ? 'Automatización generada con QA y Claude' : lang === 'en' ? 'Automation generated with QA and Claude' : 'Automação gerada com QA e Claude'}
                </h3>
                <p class="text-gray-600 dark:text-gray-400">
                  {lang === 'es'
                    ? 'Implementamos automatización inteligente de QA utilizando Claude AI para generar casos de prueba, scripts de testing y validación automatizada de calidad.'
                    : lang === 'en'
                    ? 'We implement intelligent QA automation using Claude AI to generate test cases, testing scripts, and automated quality validation.'
                    : 'Implementamos automação inteligente de QA utilizando Claude AI para gerar casos de teste, scripts de testing e validação automatizada de qualidade.'}
                </p>
              </div>
            </div>
          </div>

          <div class="border-t-2 border-gray-200 dark:border-gray-800"></div>

          <!-- Advisory Form Section -->
          <div class="space-y-8">
            <div class="text-center">
              <h2 class="text-3xl md:text-4xl font-black text-gray-900 dark:text-white mb-4" style="font-family: 'Inter Tight', system-ui, sans-serif;">
                {lang === 'es' ? '¿En qué podemos ayudarte?' : lang === 'en' ? 'How can we help you?' : 'Como podemos ajudá-lo?'}
              </h2>
              <p class="text-lg text-gray-600 dark:text-gray-400">
                {lang === 'es'
                  ? 'Selecciona el tipo de asesoría que necesitas y cuéntanos más sobre tu proyecto'
                  : lang === 'en'
                  ? 'Select the type of advisory you need and tell us more about your project'
                  : 'Selecione o tipo de assessoria que você precisa e nos conte mais sobre seu projeto'}
              </p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
              <!-- Formulario (2/3 del ancho en pantallas grandes) -->
              <form id="advisory-form" class="lg:col-span-2 w-full space-y-6">

              <!-- Campos personales -->
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <label for="advisory-name" class="block text-sm font-semibold text-gray-900 dark:text-white mb-2">
                    {lang === 'es' ? 'Nombre:' : lang === 'en' ? 'Name:' : 'Nome:'}
                  </label>
                  <input
                    type="text"
                    id="advisory-name"
                    name="name"
                    placeholder={lang === 'es' ? 'Tu nombre completo' : lang === 'en' ? 'Your full name' : 'Seu nome completo'}
                    class="w-full px-4 py-3 border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-[#1a1a1a] rounded-xl text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:border-primary focus:ring-2 focus:ring-primary/20 transition-all"
                    required
                  />
                </div>
                <div>
                  <label for="advisory-phone" class="block text-sm font-semibold text-gray-900 dark:text-white mb-2">
                    {lang === 'es' ? 'Teléfono:' : lang === 'en' ? 'Phone:' : 'Telefone:'}
                  </label>
                  <input
                    type="tel"
                    id="advisory-phone"
                    name="phone"
                    placeholder={lang === 'es' ? '+52 123 456 7890' : lang === 'en' ? '+1 123 456 7890' : '+55 123 456 7890'}
                    class="w-full px-4 py-3 border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-[#1a1a1a] rounded-xl text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:border-primary focus:ring-2 focus:ring-primary/20 transition-all"
                    required
                  />
                </div>
              </div>

              <div>
                <label for="advisory-email" class="block text-sm font-semibold text-gray-900 dark:text-white mb-2">
                  {lang === 'es' ? 'Correo electrónico:' : lang === 'en' ? 'Email:' : 'E-mail:'}
                </label>
                <input
                  type="email"
                  id="advisory-email"
                  name="email"
                  placeholder={lang === 'es' ? 'tu@email.com' : lang === 'en' ? 'your@email.com' : 'seu@email.com'}
                  class="w-full px-4 py-3 border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-[#1a1a1a] rounded-xl text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:border-primary focus:ring-2 focus:ring-primary/20 transition-all"
                  required
                />
              </div>

              <!-- Grid de opciones -->
              <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                <!-- Opción 1: Proyecto nuevo -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="new_project" class="peer sr-only" required />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Tengo una idea nueva' : lang === 'en' ? 'I have a new idea' : 'Tenho uma nova ideia'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 2: Mejorar existente -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="improve_existing" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Mejorar lo que tengo' : lang === 'en' ? 'Improve what I have' : 'Melhorar o que tenho'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 3: Escalar negocio -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="scale_business" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Crecer mi negocio' : lang === 'en' ? 'Grow my business' : 'Crescer meu negócio'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 4: IA -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="ai_integration" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Usar Inteligencia Artificial' : lang === 'en' ? 'Use Artificial Intelligence' : 'Usar Inteligência Artificial'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 5: Resolver problemas -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="fix_problems" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Resolver problemas' : lang === 'en' ? 'Fix problems' : 'Resolver problemas'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 6: Guiar equipo -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="guide_team" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Guiar mi equipo técnico' : lang === 'en' ? 'Guide my tech team' : 'Guiar minha equipe técnica'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 7: Proyecto estancado -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="project_stuck" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Mi proyecto está estancado' : lang === 'en' ? 'My project is stuck' : 'Meu projeto está estagnado'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 8: Presupuesto limitado -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="limited_budget" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Tengo presupuesto limitado' : lang === 'en' ? 'I have limited budget' : 'Tenho orçamento limitado'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 9: Validar idea -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="validate_idea" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Validar mi idea de negocio' : lang === 'en' ? 'Validate my business idea' : 'Validar minha ideia de negócio'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 10: Migrar tecnología -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="migrate_tech" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Actualizar tecnología antigua' : lang === 'en' ? 'Update old technology' : 'Atualizar tecnologia antiga'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 11: Seguridad -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="security" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Mejorar la seguridad' : lang === 'en' ? 'Improve security' : 'Melhorar a segurança'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 12: Integración -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="integration" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Conectar mis sistemas' : lang === 'en' ? 'Connect my systems' : 'Conectar meus sistemas'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 13: Mentoring -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="mentoring" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'Mentoring a la medida' : lang === 'en' ? 'Custom mentoring' : 'Mentoria personalizada'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>

                <!-- Opción 14: No estoy seguro -->
                <label class="relative flex items-center gap-3 px-4 py-3 bg-white dark:bg-[#1a1a1a] border-2 border-gray-200 dark:border-gray-700 rounded-xl cursor-pointer hover:border-primary transition-all duration-300 group">
                  <input type="radio" name="advisory-type" value="not_sure" class="peer sr-only" />
                  <div class="w-5 h-5 rounded-full border-2 border-gray-300 dark:border-gray-600 peer-checked:border-primary peer-checked:bg-primary transition-all duration-300 flex items-center justify-center flex-shrink-0">
                    <div class="w-2 h-2 bg-white rounded-full opacity-0 peer-checked:opacity-100"></div>
                  </div>
                  <svg class="w-5 h-5 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  <span class="text-gray-700 dark:text-gray-300 font-medium text-sm peer-checked:text-primary transition-colors text-left">
                    {lang === 'es' ? 'No estoy seguro' : lang === 'en' ? 'Not sure' : 'Não tenho certeza'}
                  </span>
                  <div class="absolute inset-0 bg-primary/5 rounded-xl opacity-0 peer-checked:opacity-100 transition-opacity -z-10"></div>
                </label>
              </div>

              <!-- Textarea descripción -->
              <div>
                <label for="advisory-description" class="block text-sm font-semibold text-gray-900 dark:text-white mb-2">
                  {lang === 'es' ? 'Cuéntanos más sobre tu proyecto:' : lang === 'en' ? 'Tell us more about your project:' : 'Conte-nos mais sobre seu projeto:'}
                </label>
                <textarea
                  id="advisory-description"
                  name="description"
                  placeholder={lang === 'es' ? 'Describe brevemente tu proyecto, objetivos, y cualquier detalle relevante...' : lang === 'en' ? 'Briefly describe your project, goals, and any relevant details...' : 'Descreva brevemente seu projeto, objetivos e quaisquer detalhes relevantes...'}
                  class="w-full px-4 py-3 h-32 border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-[#1a1a1a] rounded-xl text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:border-primary focus:ring-2 focus:ring-primary/20 transition-all resize-none"
                  required
                ></textarea>
              </div>

              <!-- Submit button -->
              <button
                type="submit"
                id="advisory-submit-btn"
                class="w-full px-6 py-4 bg-gradient-to-r from-primary to-purple-600 text-white font-bold text-lg rounded-xl transition-all duration-300 hover:shadow-xl hover:scale-[1.02] disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100"
                style="font-family: 'Inter Tight', system-ui, sans-serif;"
                disabled
              >
                {lang === 'es' ? 'Completa todos los campos' : lang === 'en' ? 'Complete all fields' : 'Preencha todos os campos'}
              </button>

              <!-- Privacy note -->
              <p class="text-xs text-center text-gray-500 dark:text-gray-400">
                {lang === 'es'
                  ? 'Al enviar este formulario, aceptas nuestra'
                  : lang === 'en'
                  ? 'By submitting this form, you accept our'
                  : 'Ao enviar este formulário, você aceita nossa'}
                {' '}
                <a href={`/${lang === 'es' ? '' : lang}/privacy-policy`} class="text-primary hover:underline">
                  {lang === 'es' ? 'Política de Privacidad' : lang === 'en' ? 'Privacy Policy' : 'Política de Privacidade'}
                </a>
              </p>
              </form>

              <!-- Texto lateral (1/3 del ancho en pantallas grandes) -->
              <div class="lg:col-span-1">
                <div class="bg-gradient-to-br from-primary/10 to-purple-600/10 dark:from-primary/20 dark:to-purple-600/20 rounded-2xl p-6 lg:p-8 space-y-4 sticky top-4">
                  <div class="text-4xl mb-4">🚀</div>
                  <h3 class="text-2xl font-bold text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
                    {lang === 'es' ? 'Potencia tu proyecto' : lang === 'en' ? 'Empower your project' : 'Potencialize seu projeto'}
                  </h3>
                  <p class="text-gray-700 dark:text-gray-300 leading-relaxed">
                    {lang === 'es'
                      ? 'Potencia tu proyecto con nuestra experiencia en diseño de sistemas escalables, mantenibles y de alto rendimiento. Nuestro equipo de arquitectos te guiará hacia las mejores prácticas y tecnologías.'
                      : lang === 'en'
                      ? 'Empower your project with our expertise in designing scalable, maintainable, and high-performance systems. Our team of architects will guide you towards best practices and technologies.'
                      : 'Potencialize seu projeto com nossa experiência em design de sistemas escaláveis, mantíveis e de alto desempenho. Nossa equipe de arquitetos o guiará para as melhores práticas e tecnologias.'}
                  </p>
                  <div class="space-y-3 pt-4">
                    <div class="flex items-start gap-3">
                      <span class="text-primary font-bold text-xl">✓</span>
                      <span class="text-gray-700 dark:text-gray-300">
                        {lang === 'es' ? 'Consultoría personalizada' : lang === 'en' ? 'Personalized consulting' : 'Consultoria personalizada'}
                      </span>
                    </div>
                    <div class="flex items-start gap-3">
                      <span class="text-primary font-bold text-xl">✓</span>
                      <span class="text-gray-700 dark:text-gray-300">
                        {lang === 'es' ? 'Arquitectos expertos' : lang === 'en' ? 'Expert architects' : 'Arquitetos especialistas'}
                      </span>
                    </div>
                    <div class="flex items-start gap-3">
                      <span class="text-primary font-bold text-xl">✓</span>
                      <span class="text-gray-700 dark:text-gray-300">
                        {lang === 'es' ? 'Mejores prácticas' : lang === 'en' ? 'Best practices' : 'Melhores práticas'}
                      </span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="border-t-2 border-gray-200 dark:border-gray-800"></div>

          <!-- CTA Final -->
          <div class="bg-gradient-to-br from-primary to-purple-600 rounded-3xl p-8 md:p-12 text-center text-white">
            <h2 class="text-3xl md:text-4xl font-black mb-4" style="font-family: 'Inter Tight', system-ui, sans-serif;">
              {lang === 'es' ? '¿Listo para escalar tu proyecto?' : lang === 'en' ? 'Ready to scale your project?' : 'Pronto para escalar seu projeto?'}
            </h2>
            <p class="text-xl mb-8 text-white/90">
              {lang === 'es'
                ? 'Agenda una consulta gratuita de 30 minutos'
                : lang === 'en'
                ? 'Schedule a free 30-minute consultation'
                : 'Agende uma consulta gratuita de 30 minutos'}
            </p>
            <a
              href={`/${lang === 'es' ? '' : lang}/#contact`}
              class="inline-block px-8 py-4 bg-white text-gray-900 font-bold text-lg rounded-full hover:scale-105 transition-all duration-300 shadow-xl"
              style="font-family: 'Inter Tight', system-ui, sans-serif;"
            >
              {lang === 'es' ? 'Contactar Ahora' : lang === 'en' ? 'Contact Now' : 'Contatar Agora'}
            </a>
          </div>
        </div>
      </section>
    </main>

    <!-- Modales -->
    <!-- Modal Hexagonal -->
    <div id="modal-hexagonal" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden items-center justify-center p-4">
      <div class="bg-white dark:bg-[#1a1a1a] rounded-3xl max-w-2xl w-full max-h-[90vh] overflow-y-auto">
        <div class="p-8 space-y-6">
          <div class="flex items-start justify-between">
            <div class="flex items-center gap-4">
              <span class="text-5xl">🏗️</span>
              <h3 class="text-3xl font-bold text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
                {lang === 'es' ? 'Arquitectura Hexagonal' : lang === 'en' ? 'Hexagonal Architecture' : 'Arquitetura Hexagonal'}
              </h3>
            </div>
            <button onclick="closeModal('modal-hexagonal')" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 text-3xl leading-none">
              ×
            </button>
          </div>

          <div class="space-y-4">
            <p class="text-lg font-semibold text-gray-900 dark:text-white">
              {lang === 'es' ? 'Ports & Adapters' : lang === 'en' ? 'Ports & Adapters' : 'Ports & Adapters'}
            </p>

            <div>
              <p class="font-semibold text-gray-900 dark:text-white mb-2">
                {lang === 'es' ? '¿Para qué sirve?' : lang === 'en' ? 'What is it for?' : 'Para que serve?'}
              </p>
              <p class="text-gray-700 dark:text-gray-300">
                {lang === 'es'
                  ? 'Tu lógica de negocio no depende de frameworks ni databases. Puedes cambiar de Express a Fastify, de MySQL a PostgreSQL, sin tocar el core.'
                  : lang === 'en'
                  ? 'Your business logic doesn\'t depend on frameworks or databases. You can switch from Express to Fastify, from MySQL to PostgreSQL, without touching the core.'
                  : 'Sua lógica de negócio não depende de frameworks ou databases. Você pode mudar de Express para Fastify, de MySQL para PostgreSQL, sem tocar o core.'}
              </p>
            </div>

            <div>
              <p class="font-semibold text-gray-900 dark:text-white mb-2">
                {lang === 'es' ? 'Ideal para:' : lang === 'en' ? 'Ideal for:' : 'Ideal para:'}
              </p>
              <ul class="space-y-2 text-gray-700 dark:text-gray-300">
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Startups que pivotean frecuentemente' : lang === 'en' ? 'Startups that pivot frequently' : 'Startups que pivotam frequentemente'}</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Productos con múltiples integraciones' : lang === 'en' ? 'Products with multiple integrations' : 'Produtos com múltiplas integrações'}</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Equipos que quieren testear fácil' : lang === 'en' ? 'Teams that want easy testing' : 'Equipes que querem testar fácil'}</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal Clean Architecture -->
    <div id="modal-clean" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden items-center justify-center p-4">
      <div class="bg-white dark:bg-[#1a1a1a] rounded-3xl max-w-2xl w-full max-h-[90vh] overflow-y-auto">
        <div class="p-8 space-y-6">
          <div class="flex items-start justify-between">
            <div class="flex items-center gap-4">
              <span class="text-5xl">🧅</span>
              <h3 class="text-3xl font-bold text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
                {lang === 'es' ? 'Clean Architecture' : lang === 'en' ? 'Clean Architecture' : 'Clean Architecture'}
              </h3>
            </div>
            <button onclick="closeModal('modal-clean')" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 text-3xl leading-none">
              ×
            </button>
          </div>

          <div class="space-y-4">
            <p class="text-lg font-semibold text-gray-900 dark:text-white">
              {lang === 'es' ? 'Onion Architecture' : lang === 'en' ? 'Onion Architecture' : 'Onion Architecture'}
            </p>

            <div>
              <p class="font-semibold text-gray-900 dark:text-white mb-2">
                {lang === 'es' ? '¿Para qué sirve?' : lang === 'en' ? 'What is it for?' : 'Para que serve?'}
              </p>
              <p class="text-gray-700 dark:text-gray-300">
                {lang === 'es'
                  ? 'Código organizado en capas con dependencias en una sola dirección. El core del negocio está en el centro, protegido de cambios externos.'
                  : lang === 'en'
                  ? 'Code organized in layers with dependencies in one direction only. The business core is in the center, protected from external changes.'
                  : 'Código organizado em camadas com dependências em uma única direção. O core do negócio está no centro, protegido de mudanças externas.'}
              </p>
            </div>

            <div>
              <p class="font-semibold text-gray-900 dark:text-white mb-2">
                {lang === 'es' ? 'Ideal para:' : lang === 'en' ? 'Ideal for:' : 'Ideal para:'}
              </p>
              <ul class="space-y-2 text-gray-700 dark:text-gray-300">
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Proyectos enterprise complejos' : lang === 'en' ? 'Complex enterprise projects' : 'Projetos enterprise complexos'}</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Equipos grandes (10+ devs)' : lang === 'en' ? 'Large teams (10+ devs)' : 'Equipes grandes (10+ devs)'}</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Sistemas que vivirán 5+ años' : lang === 'en' ? 'Systems that will live 5+ years' : 'Sistemas que viverão 5+ anos'}</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal Microservicios -->
    <div id="modal-microservices" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden items-center justify-center p-4">
      <div class="bg-white dark:bg-[#1a1a1a] rounded-3xl max-w-2xl w-full max-h-[90vh] overflow-y-auto">
        <div class="p-8 space-y-6">
          <div class="flex items-start justify-between">
            <div class="flex items-center gap-4">
              <span class="text-5xl">🔷</span>
              <h3 class="text-3xl font-bold text-gray-900 dark:text-white" style="font-family: 'Inter Tight', system-ui, sans-serif;">
                {lang === 'es' ? 'Microservicios' : lang === 'en' ? 'Microservices' : 'Microserviços'}
              </h3>
            </div>
            <button onclick="closeModal('modal-microservices')" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 text-3xl leading-none">
              ×
            </button>
          </div>

          <div class="space-y-4">
            <p class="text-lg font-semibold text-gray-900 dark:text-white">
              {lang === 'es' ? 'Servicios independientes' : lang === 'en' ? 'Independent services' : 'Serviços independentes'}
            </p>

            <div>
              <p class="font-semibold text-gray-900 dark:text-white mb-2">
                {lang === 'es' ? '¿Para qué sirve?' : lang === 'en' ? 'What is it for?' : 'Para que serve?'}
              </p>
              <p class="text-gray-700 dark:text-gray-300">
                {lang === 'es'
                  ? 'Divide tu aplicación en servicios pequeños e independientes. Cada uno puede deployarse por separado, escalar solo lo que necesita, usar tecnologías diferentes.'
                  : lang === 'en'
                  ? 'Divide your application into small, independent services. Each can be deployed separately, scale only what it needs, use different technologies.'
                  : 'Divida sua aplicação em serviços pequenos e independentes. Cada um pode ser deployado separadamente, escalar apenas o necessário, usar tecnologias diferentes.'}
              </p>
            </div>

            <div>
              <p class="font-semibold text-gray-900 dark:text-white mb-2">
                {lang === 'es' ? 'Ideal para:' : lang === 'en' ? 'Ideal for:' : 'Ideal para:'}
              </p>
              <ul class="space-y-2 text-gray-700 dark:text-gray-300">
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Plataformas con múltiples funcionalidades' : lang === 'en' ? 'Platforms with multiple functionalities' : 'Plataformas com múltiplas funcionalidades'}</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'Equipos distribuidos' : lang === 'en' ? 'Distributed teams' : 'Equipes distribuídas'}</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="text-primary">✓</span>
                  <span>{lang === 'es' ? 'SaaS que cobra por módulos' : lang === 'en' ? 'SaaS that charges per module' : 'SaaS que cobra por módulos'}</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>

<script is:inline>
  // Funciones para manejar modales
  function openModal(modalId) {
    const modal = document.getElementById(modalId);
    if (modal) {
      modal.classList.remove('hidden');
      modal.classList.add('flex');
      document.body.style.overflow = 'hidden';
    }
  }

  function closeModal(modalId) {
    const modal = document.getElementById(modalId);
    if (modal) {
      modal.classList.add('hidden');
      modal.classList.remove('flex');
      document.body.style.overflow = 'auto';
    }
  }

  // Cerrar modal al hacer clic fuera de él
  document.addEventListener('click', (e) => {
    if (e.target.classList.contains('backdrop-blur-sm')) {
      const modals = ['modal-hexagonal', 'modal-clean', 'modal-microservices'];
      modals.forEach(modalId => closeModal(modalId));
    }
  });

  // Cerrar modal con tecla ESC
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      const modals = ['modal-hexagonal', 'modal-clean', 'modal-microservices'];
      modals.forEach(modalId => closeModal(modalId));
    }
  });
</script>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('advisory-form') as HTMLFormElement;
    const submitBtn = document.getElementById('advisory-submit-btn') as HTMLButtonElement;
    const radioButtons = document.querySelectorAll<HTMLInputElement>('input[name="advisory-type"]');
    const nameInput = document.getElementById('advisory-name') as HTMLInputElement;
    const phoneInput = document.getElementById('advisory-phone') as HTMLInputElement;
    const emailInput = document.getElementById('advisory-email') as HTMLInputElement;
    const descriptionTextarea = document.getElementById('advisory-description') as HTMLTextAreaElement;

    // Detectar el idioma desde la URL
    const path = window.location.pathname;
    const lang = path.includes('/en/') ? 'en' : path.includes('/pt/') ? 'pt' : 'es';

    // Textos según el idioma
    const texts = {
      es: {
        disabled: 'Completa todos los campos',
        enabled: 'Enviar solicitud de asesoría',
        sending: 'Enviando...',
        success: '¡Gracias! Te contactaremos pronto.',
        error: 'Error al enviar. Por favor, intenta de nuevo.'
      },
      en: {
        disabled: 'Complete all fields',
        enabled: 'Send advisory request',
        sending: 'Sending...',
        success: 'Thank you! We will contact you soon.',
        error: 'Error sending. Please try again.'
      },
      pt: {
        disabled: 'Preencha todos os campos',
        enabled: 'Enviar solicitação de assessoria',
        sending: 'Enviando...',
        success: 'Obrigado! Entraremos em contato em breve.',
        error: 'Erro ao enviar. Por favor, tente novamente.'
      }
    };

    const t = texts[lang as keyof typeof texts];

    function checkFormValidity() {
      const isRadioSelected = Array.from(radioButtons).some(radio => radio.checked);
      const isNameFilled = nameInput.value.trim().length > 0;
      const isPhoneFilled = phoneInput.value.trim().length > 0;
      const isEmailFilled = emailInput.value.trim().length > 0;
      const isDescriptionFilled = descriptionTextarea.value.trim().length > 0;

      if (isRadioSelected && isNameFilled && isPhoneFilled && isEmailFilled && isDescriptionFilled) {
        submitBtn.disabled = false;
        submitBtn.textContent = t.enabled;
        submitBtn.classList.remove('opacity-50', 'cursor-not-allowed');
      } else {
        submitBtn.disabled = true;
        submitBtn.textContent = t.disabled;
        submitBtn.classList.add('opacity-50', 'cursor-not-allowed');
      }
    }

    // Event listeners para los radio buttons
    radioButtons.forEach(radio => {
      radio.addEventListener('change', checkFormValidity);
    });

    // Event listeners para los campos de texto
    nameInput.addEventListener('input', checkFormValidity);
    phoneInput.addEventListener('input', checkFormValidity);
    emailInput.addEventListener('input', checkFormValidity);
    descriptionTextarea.addEventListener('input', checkFormValidity);

    // Manejar el submit del formulario
    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const selectedRadio = Array.from(radioButtons).find(radio => radio.checked) as HTMLInputElement;
      if (!selectedRadio) return;

      const formData = {
        advisoryType: selectedRadio.value,
        name: nameInput.value.trim(),
        phone: phoneInput.value.trim(),
        email: emailInput.value.trim(),
        description: descriptionTextarea.value.trim(),
        timestamp: new Date().toISOString(),
        language: lang
      };

      // Cambiar el estado del botón
      submitBtn.disabled = true;
      submitBtn.textContent = t.sending;

      try {
        // Aquí puedes agregar la lógica para enviar el formulario a tu backend
        console.log('Form data:', formData);

        // Simulación de envío (reemplazar con tu endpoint real)
        await new Promise(resolve => setTimeout(resolve, 1000));

        // Mostrar mensaje de éxito
        const successMessage = document.createElement('div');
        successMessage.className = 'fixed top-4 right-4 bg-green-500 text-white px-6 py-4 rounded-xl shadow-lg z-50 animate-fade-in';
        successMessage.textContent = t.success;
        document.body.appendChild(successMessage);

        // Resetear el formulario
        form.reset();
        checkFormValidity();

        // Remover el mensaje después de 5 segundos
        setTimeout(() => {
          successMessage.remove();
        }, 5000);

      } catch (error) {
        console.error('Error submitting form:', error);

        // Mostrar mensaje de error
        const errorMessage = document.createElement('div');
        errorMessage.className = 'fixed top-4 right-4 bg-red-500 text-white px-6 py-4 rounded-xl shadow-lg z-50 animate-fade-in';
        errorMessage.textContent = t.error;
        document.body.appendChild(errorMessage);

        // Remover el mensaje después de 5 segundos
        setTimeout(() => {
          errorMessage.remove();
        }, 5000);

        // Restaurar el botón
        submitBtn.disabled = false;
        submitBtn.textContent = t.enabled;
      }
    });

    // Check inicial
    checkFormValidity();
  });
</script>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.3s ease-out;
  }

  /* Mejorar la apariencia del radio button checked */
  input[type="radio"]:checked + div {
    box-shadow: 0 0 0 3px rgba(var(--primary-rgb), 0.1);
  }
</style>
