---
import { getLangFromUrl} from "../i18n/utils";
const lang = getLangFromUrl(Astro.url);
import UpcomingEvents from "../components/UpcomingEvents.astro";
---

<div class="max-w-[1400px] mx-auto">
  <div class="text-center mb-12">
    <h2
      class="text-4xl md:text-5xl font-black mb-4"
      style="font-family: 'Inter Tight', system-ui, sans-serif;"
    >
      {
        lang === "es"
          ? "Galería de Proyectos"
          : lang === "en"
            ? "Project Gallery"
            : "Galeria de Projetos"
      }
    </h2>
    <p class="text-xl text-gray-400 max-w-2xl mx-auto">
      {
        lang === "es"
          ? "Una muestra visual de algunos de mis trabajos y creaciones"
          : lang === "en"
            ? "A visual sample of some of my works and creations"
            : "Uma amostra visual de alguns dos meus trabalhos e criações."
      }
    </p>
  </div>

  <!-- Masonry Grid -->
  <div class="masonry-grid">
    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/01.png"
          alt="Project 1"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {"Innobiza web APP"}
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "Aplicación de comercio minorista de otros artículos en establecimientos especializados"
                : lang === "en"
                  ? "Retail application for specialized goods"
                  : "Aplicação de varejo para artigos especializados"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/02.JPG"
          alt="Project 2"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {"WEB APP ACADEMICA PARA PROGRMACIÓN E IA"}
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "Aplicación académica enfocada en programación y tecnología con inteligencia artificial"
                : lang === "en"
                  ? "Academic application focused on programming and technology with artificial intelligence"
                  : "Aplicação acadêmica focada em programação e tecnologia com inteligência artificial"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/03.png"
          alt="Project 3"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {
              lang === "es"
                ? "WebAPP Clinica centro médico la quinta"
                : lang === "en"
                  ? "Analytics Dashboard"
                  : "Dashboard de Análise"
            }
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "Web + Aplicación de marketing para empresa prestadora de servicios de salud"
                : lang === "en"
                  ? "Marketing application for a healthcare services company"
                  : "Aplicação de marketing para empresa prestadora de serviços de saúde"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/04.JPG"
          alt="Project 4"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {
              lang === "es"
                ? "Web-QR-APP + Laser +  E-commerce Darkhusky"
                : lang === "en"
                  ? "Web-QR-APP + Laser +  E-commerce Darkhusky"
                  : "Web-QR-APP + Laser +  E-commerce Darkhusky"
            }
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "Aplicación web E-commerce + QR Laser productos personalizados para mascotas"
                : lang === "en"
                  ? "Web E-commerce + QR Laser customized pet products"
                  : "Aplicação web E-commerce + QR Laser produtos personalizados para animais de estimação"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/05.JPG"
          alt="Project 5"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {
              lang === "es"
                ? "WebApp darwinyusef.com IA contents + Chatbot"
                : lang === "en"
                  ? "WebApp darwinyusef.com IA contents + Chatbot"
                  : "WebApp darwinyusef.com AI + Chatbot"
            }
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "Portafolio: Ing Darwin Yusef + IA Chatbot"
                : lang === "en"
                  ? "Portfolio: Eng. Darwin Yusef + IA Chatbot"
                  : "Portafolio: Ing Darwin Yusef + IA Chatbot"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/06.JPG"
          alt="Profile"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {
              lang === "es"
                ? "WEBAPP Mobile Exploración de talentos + IA contents"
                : lang === "en"
                  ? "WEBAPP Mobile Talent exploration + IA contents"
                  : "WEBAPP Mobile Exploração de talentos + conteúdo IA"
            }
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "WEB APP Mobile Psicologica para niños de exploración de talentos con IA"
                : lang === "en"
                  ? "Web App Mobile Psychological Talent Exploration Tool for Children with AI"
                  : "Aplicativo web e aplicativo móvel para exploração de talentos psicológicos em crianças com IA"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/07.png"
          alt="Profile"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {"FIXING MORAFIX REPAIR"}
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "WEBAPP Mobile Repación de equipos y servicios técnicos"
                : lang === "en"
                  ? "WEBAPP Mobile Equipment Repair and Technical Services"
                  : "Aplicativo Web Mobile; Reparo de equipamentos e serviços técnicos"
            }
          </p>
        </div>
      </div>
    </div>

    <div
      class="masonry-item cursor-hover-target"
      data-cursor-text={lang === "es"
        ? "Ver proyecto"
        : lang === "en"
          ? "View project"
          : "Ver projeto"}
    >
      <div class="masonry-card">
        <img
          src="./websites/08.JPG"
          alt="Profile"
          class="w-full h-auto rounded-2xl"
        />
        <div class="masonry-overlay">
          <h3 class="text-2xl font-bold mb-2">
            {
              lang === "es"
                ? "Doctora María Perez - WEBAPP Psicología"
                : lang === "en"
                  ? "Dr. Maria Perez - WEBAPP Psychology"
                  : "Dra. Maria Perez - Psicologia WEBAPP"
            }
          </h3>
          <p class="/90">
            {
              lang === "es"
                ? "Portfolio personal de psicología con chatbot + IA"
                : lang === "en"
                  ? "Personal psychology portfolio with chatbot + AI"
                  : "Portfólio de psicologia pessoal com chatbot + IA"
            }
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
    
  /* Masonry Gallery */
  .masonry-grid {
    column-count: 1;
    column-gap: 1.5rem;
  }

  @media (min-width: 640px) {
    .masonry-grid {
      column-count: 2;
    }
  }

  @media (min-width: 1024px) {
    .masonry-grid {
      column-count: 3;
    }
  }

  .masonry-item {
    break-inside: avoid;
    margin-bottom: 1.5rem;
    display: inline-block;
    width: 100%;
  }

  .masonry-card {
    position: relative;
    overflow: hidden;
    border-radius: 1rem;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .masonry-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
  }

  .masonry-card img {
    display: block;
    width: 100%;
    height: auto;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .masonry-card:hover img {
    transform: scale(1.05);
  }

  .masonry-overlay {
    position: absolute;
    margin-top: 20px;
    bottom: 0;
    left: 0;
    right: 0;
    color: white;
    font-weight: bold;
    padding: 2rem;
    transform: translateY(100%);
    background: rgba(0, 0, 0, 0.8) !important;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .masonry-card:hover .masonry-overlay {
    transform: translateY(0);
    background: #000000 !important;
  }

</style>
